<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-screen bg-zinc-900 p-10">
        <a href="/" class="text-white mb-3">back to home</a>
        <h1 class="text-white text-3xl">All Users</h1>

        <div class="users mt-5 flex  gap-3">
            <% if(users.length> 0){%>y
                <% users.forEach((val)=>{%>
                    <div class="user w-72 px-3 py-4 rounded-md bg-zinc-800 text-white">
                        <div class="w-full h-52 rounded-md bg-zinc-700">
                            <img src="<%= val.image %>" alt="">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter">
                            <%= val.name %>
                        </h3>
                        <p class="text-gray-400">
                            <%= val.email %>
                        </p>

                        <div class="mt-3">
                            <a href="/edit/<%= val._id %>" class="bg-white text-black p-2 rounded">Edit</a>
                            <a href="/delete/<%= val._id %>" class="bg-white text-black p-2 rounded">Delete</a>
                        </div>
                    </div>
                    <% }) %>
                        <% }else{%>
                            <p>No users found</p>
                            <% } %>

        </div>
    </div>
</body>

</html>